on:
  schedule:
    - cron: '0 0 * * *' # Run every day at midnight
jobs:
  repo-sync:
    name: repo sync
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v2

      - name: bitbake
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/bitbake.git'
          upstream: 'https://git.openembedded.org/bitbake'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: charls
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/charls.git'
          upstream: 'https://github.com/team-charls/charls.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: eigen
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/eigen.git'
          upstream: 'https://gitlab.com/libeigen/eigen.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: git-repo
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/git-repo.git'
          upstream: 'https://gerrit.googlesource.com/git-repo.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: googletest
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/googletest.git'
          upstream: 'https://github.com/google/googletest.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: libjpeg-turbo
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/libjpeg-turbo.git'
          upstream: 'https://github.com/libjpeg-turbo/libjpeg-turbo.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: linux-stable
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/linux-stable.git'
          upstream: 'https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: linux-stable-rt
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/linux-stable-rt.git'
          upstream: 'https://git.kernel.org/pub/scm/linux/kernel/git/rt/linux-stable-rt.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-freescale
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-freescale.git'
          upstream: 'https://github.com/Freescale/meta-freescale.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-freescale-3rdparty
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-freescale-3rdparty.git'
          upstream: 'https://github.com/Freescale/meta-freescale-3rdparty.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-freescale-distro
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-freescale-distro.git'
          upstream: 'https://github.com/Freescale/meta-freescale-distro.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-intel
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-intel.git'
          upstream: 'https://git.yoctoproject.org/git/meta-intel.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-intel-aero-base
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-intel-aero-base.git'
          upstream: 'https://github.com/intel-aero/meta-intel-aero-base.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-openembedded
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-openembedded.git'
          upstream: 'https://git.openembedded.org/meta-openembedded'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-raspberrypi
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-raspberrypi.git'
          upstream: 'https://github.com/agherzan/meta-raspberrypi.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-swupdate
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-swupdate.git'
          upstream: 'https://github.com/sbabic/meta-swupdate.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-variscite-imx
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-variscite-imx.git'
          upstream: 'https://github.com/varigit/meta-variscite-imx.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: meta-virtualization
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/meta-virtualization.git'
          upstream: 'https://git.yoctoproject.org/git/meta-virtualization.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: nlopt
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/nlopt.git'
          upstream: 'https://github.com/stevengj/nlopt.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: OpenCL-ICD-Loader
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/OpenCL-ICD-Loader.git'
          upstream: 'https://github.com/KhronosGroup/OpenCL-ICD-Loader.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: opencv
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/opencv.git'
          upstream: 'https://github.com/opencv/opencv.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: opencv_contrib
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/opencv_contrib.git'
          upstream: 'https://github.com/opencv/opencv_contrib.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: openembedded-core
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/openembedded-core.git'
          upstream: 'https://git.openembedded.org/openembedded-core'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: openssl
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/openssl.git'
          upstream: 'https://github.com/openssl/openssl.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: openexr
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/openexr.git'
          upstream: 'https://github.com/AcademySoftwareFoundation/openexr.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: poky
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/poky.git'
          upstream: 'https://git.yoctoproject.org/git/poky.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: pybind11
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/pybind11.git'
          upstream: 'https://github.com/pybind/pybind11.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: tinyxml2
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/tinyxml2.git'
          upstream: 'https://github.com/leethomason/tinyxml2.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qt3d
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qt3d.git'
          upstream: 'https://github.com/qt/qt3d.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qt5
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qt5.git'
          upstream: 'https://github.com/qt/qt5.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qt5compat
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qt5compat.git'
          upstream: 'https://github.com/qt/qt5compat.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtactiveqt
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtactiveqt.git'
          upstream: 'https://github.com/qt/qtactiveqt.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtandroidextras
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtandroidextras.git'
          upstream: 'https://github.com/qt/qtandroidextras.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtbase
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtbase.git'
          upstream: 'https://github.com/qt/qtbase.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtcanvas3d
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtcanvas3d.git'
          upstream: 'https://github.com/qt/qtcanvas3d.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtconnectivity
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtconnectivity.git'
          upstream: 'https://github.com/qt/qtconnectivity.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtcharts
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtcharts.git'
          upstream: 'https://github.com/qt/qtcharts.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtdatavis3d
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtdatavis3d.git'
          upstream: 'https://github.com/qt/qtdatavis3d.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtdeclarative
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtdeclarative.git'
          upstream: 'https://github.com/qt/qtdeclarative.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtdeclarative-testsuites
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtdeclarative-testsuites.git'
          upstream: 'https://github.com/qt/qtdeclarative-testsuites.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtdoc
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtdoc.git'
          upstream: 'https://github.com/qt/qtdoc.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtdocgallery
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtdocgallery.git'
          upstream: 'https://github.com/qt/qtdocgallery.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtfeedback
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtfeedback.git'
          upstream: 'https://github.com/qt/qtfeedback.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtgamepad
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtgamepad.git'
          upstream: 'https://github.com/qt/qtgamepad.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtgraphicaleffects
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtgraphicaleffects.git'
          upstream: 'https://github.com/qt/qtgraphicaleffects.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtimageformats
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtimageformats.git'
          upstream: 'https://github.com/qt/qtimageformats.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtlocation
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtlocation.git'
          upstream: 'https://github.com/qt/qtlocation.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtlocation-mapboxgl
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtlocation-mapboxgl.git'
          upstream: 'https://github.com/qt/qtlocation-mapboxgl.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtlottie
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtlottie.git'
          upstream: 'https://github.com/qt/qtlottie.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtmacextras
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtmacextras.git'
          upstream: 'https://github.com/qt/qtmacextras.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtmultimedia
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtmultimedia.git'
          upstream: 'https://github.com/qt/qtmultimedia.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtnetworkauth
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtnetworkauth.git'
          upstream: 'https://github.com/qt/qtnetworkauth.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtpim
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtpim.git'
          upstream: 'https://github.com/qt/qtpim.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtpurchasing
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtpurchasing.git'
          upstream: 'https://github.com/qt/qtpurchasing.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtrepotools
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtrepotools.git'
          upstream: 'https://github.com/qt/qtrepotools.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtqa
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtqa.git'
          upstream: 'https://github.com/qt/qtqa.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtquick3d
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtquick3d.git'
          upstream: 'https://github.com/qt/qtquick3d.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtquickcontrols
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtquickcontrols.git'
          upstream: 'https://github.com/qt/qtquickcontrols.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtquickcontrols2
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtquickcontrols2.git'
          upstream: 'https://github.com/qt/qtquickcontrols2.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtquicktimeline
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtquicktimeline.git'
          upstream: 'https://github.com/qt/qtquicktimeline.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtremoteobjects
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtremoteobjects.git'
          upstream: 'https://github.com/qt/qtremoteobjects.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtscxml
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtscxml.git'
          upstream: 'https://github.com/qt/qtscxml.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtscript
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtscript.git'
          upstream: 'https://github.com/qt/qtscript.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtsensors
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtsensors.git'
          upstream: 'https://github.com/qt/qtsensors.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtserialbus
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtserialbus.git'
          upstream: 'https://github.com/qt/qtserialbus.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtserialport
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtserialport.git'
          upstream: 'https://github.com/qt/qtserialport.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtshadertools
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtshadertools.git'
          upstream: 'https://github.com/qt/qtshadertools.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtspeech
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtspeech.git'
          upstream: 'https://github.com/qt/qtspeech.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtsvg
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtsvg.git'
          upstream: 'https://github.com/qt/qtsvg.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtsystems
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtsystems.git'
          upstream: 'https://github.com/qt/qtsystems.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qttools
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qttools.git'
          upstream: 'https://github.com/qt/qttools.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qttranslations
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qttranslations.git'
          upstream: 'https://github.com/qt/qttranslations.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtvirtualkeyboard
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtvirtualkeyboard.git'
          upstream: 'https://github.com/qt/qtvirtualkeyboard.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwayland
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwayland.git'
          upstream: 'https://github.com/qt/qtwayland.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebchannel
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebchannel.git'
          upstream: 'https://github.com/qt/qtwebchannel.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebengine
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebengine.git'
          upstream: 'https://github.com/qt/qtwebengine.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebengine-chromium
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebengine-chromium.git'
          upstream: 'https://github.com/qt/qtwebengine-chromium.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebglplugin
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebglplugin.git'
          upstream: 'https://github.com/qt/qtwebglplugin.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebsockets
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebsockets.git'
          upstream: 'https://github.com/qt/qtwebsockets.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwebview
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwebview.git'
          upstream: 'https://github.com/qt/qtwebview.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtwinextras
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtwinextras.git'
          upstream: 'https://github.com/qt/qtwinextras.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtx11extras
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtx11extras.git'
          upstream: 'https://github.com/qt/qtx11extras.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtxmlpatterns
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtxmlpatterns.git'
          upstream: 'https://github.com/qt/qtxmlpatterns.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: qtxmlpatterns-testsuites
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/qtxmlpatterns-testsuites.git'
          upstream: 'https://github.com/qt/qtxmlpatterns-testsuites.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: spdlog
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/spdlog.git'
          upstream: 'https://github.com/gabime/spdlog.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: swupdate
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/swupdate.git'
          upstream: 'https://github.com/sbabic/swupdate.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: thrift
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/thrift.git'
          upstream: 'https://github.com/apache/thrift.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: u-boot
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/u-boot.git'
          upstream: 'https://github.com/u-boot/u-boot.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: wcelibcex
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/wcelibcex.git'
          upstream: 'https://github.com/krestenkrab/wcelibcex.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: wkhtmltopdf
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/wkhtmltopdf.git'
          upstream: 'https://github.com/wkhtmltopdf/wkhtmltopdf.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: zlib
        uses: ./
        with:
          origin: 'https://github.com/hexagon-geo-surv/zlib.git'
          upstream: 'https://github.com/madler/zlib.git'
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
